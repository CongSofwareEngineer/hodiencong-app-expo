import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    display: 'flex',
    flexDirection: 'column',
    gap: 20,
  },
  containerItem: {
    display: 'flex',
    flexDirection: 'column',
    gap: 10,
  },

  containerItemSub: {
    display: 'flex',
    flexDirection: 'row',
    gap: 10,
  },
})
